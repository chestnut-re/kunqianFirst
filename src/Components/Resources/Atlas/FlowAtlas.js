import React, {Component} from 'react';
import {Tabs,Form} from 'antd';
const { TabPane } = Tabs;
class FlowAtlas extends Component {
    constructor() {
        super()
        this.state = {

        }
    }
    getFormContent(){

    }
    shouldComponentUpdate(nextProps, nextState, nextContext) {

    }
    generteElement() {
        return <Tabs>

        </Tabs>
    }
    render() {
        return (
            <div>
                <Form>

                </Form>
            </div>
        );
    }
}

export default Form.create()(FlowAtlas);